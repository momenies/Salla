# AGENTS.md

## حالة المشروع

- الهدف: بناء عدة تطبيقات صغيرة مدفوعة باشتراك لنشرها على متجر تطبيقات سلة (Salla App Store)، بإعادة استخدام قالب واحد.
- `template/` هو القالب الأساسي: نسخة معدلة من قالب سلة الرسمي `SallaApp/express-starter-kit` (Node.js + Express). أحداث الـ webhooks في `template/Actions/`.
- **تم اختبار تدفق تسجيل الدخول OAuth كاملاً وهو يعمل** (متجر تجريبي في بوابة الشركاء). تعديلاتنا على القالب الرسمي (لا تُفقد عند التحديث):
  - `express` مثبت على `4.21.2` (الإصدار 5 يكسر `setExpressVerify` في مكتبة سلة).
  - قاعدة البيانات: SQLite محلية عبر متغير `DATABASE_STORAGE=database.sqlite` في `.env` (بدل MySQL) — للإنتاج لاحقاً نستبدلها.
  - إصلاح خطأين في `database/index.js` (saveOauth كانت تبحث بـ email غير موجود، retrieveUser لم تكن تجلب التوكن) وحماية مسار `/` في `app.js` من الانهيار.
- يتطلب Node >= 20.19 (المثبت على الجهاز: v24). تشغيل محلي: `npm install` ثم `npm run dev` داخل `template/`.
- مفاتيح التطبيق الحقيقية (Client ID/Secret) موجودة في `template/.env` — الملف مستثنى من Git عبر `.gitignore`، لا ترفعه أبداً ولا تطبعه في اللوج.
- Webhook Secret و App ID ما زالا غير مضبوطين في `.env` (مطلوبان لأحداث المتجر لاحقاً).
- النشر المخطط: Google Cloud Run (لدى المستخدم رصيد GCP). عند النشر يتغير Callback URL في البوابة إلى الرابط العام.
- المستودع Git جاهز: الفرع `main`، والجهاز البعيد `origin` هو `https://github.com/momenies/opencode.git`.

## بنية التطبيق

القالب أُعيد تنظيمه من ملف `app.js` واحد إلى طبقات واضحة داخل `template/`:

```
app.js            الإقلاع فقط: إعدادات → قاعدة بيانات → قوالب → طبقة وسطى → مسارات → أخطاء
config/           كل متغيّرات البيئة في مكان واحد + فحص عند الإقلاع (inspect)
lib/              salla.js (عميل واجهة سلة) · auth.js (OAuth) · format.js (فلاتر القوالب) · normalize.js
services/         db.js (اتصال واحد مشترك) · stores.js (المتاجر والتوكنات والأحداث والاشتراك)
middleware/       أمان · تسجيل · مصادقة · requireStore (حارس الصفحات المحمية)
routes/           ملف لكل قسم، يُجمَّع في routes/index.js
views/            القوالب + partials/ للأجزاء المشتركة
public/           css/app.css و js/app.js — بلا مكتبات خارجية ولا خطوة بناء
test/             `npm test` — 39 اختباراً تشمل تجديد التوكن والويبهوك وقاعدة البيانات
```

### لماذا لا نستخدم دوال المكتبة الرسمية لجلب البيانات؟

`@salla.sa/passport-strategy` تحتفظ بتوكن **واحد** داخل ذاكرة العملية كلها
(`SallaAPI.getAllOrders()` تقرأ من متغيّر عام). مع تاجرَين في نفس الوقت يدوس
أحدهما على توكن الآخر، و`logout` يمسح التوكن للجميع. لذلك:

- المكتبة تُستخدم **لتسجيل الدخول فقط** (`lib/auth.js`).
- كل جلب بيانات يمرّ عبر `lib/salla.js` — عميل يأخذ توكن متجره من قاعدة
  البيانات، ويجدّده تلقائياً عند 401، ويحفظ الجديد.
- حُذف `SallaAPI.setExpressVerify` لأنه كان **يستبدل `req.query` بالكامل**
  فتضيع معاملات البحث والصفحات، إضافة إلى مشكلة التوكن المشترك.
  (يعني قيد تثبيت express على 4.21.2 لم يعد ضرورياً، لكننا لم نرفعه في هذه المرحلة.)

### الأقسام

الرئيسية (إحصاءات + مخطّط مبيعات ١٤ يوماً + آخر الطلبات) · الطلبات (بحث/فلترة/ترقيم
+ صفحة تفاصيل) · العملاء · المنتجات · الأدوات (حاسبة تسعير وضريبة، رسائل واتساب،
تصدير CSV، فحص حالة) · سجل الأحداث · الإعدادات · الاشتراك · بيانات الحساب · تجديد التوكن.

### الإعدادات والاشتراك

- إعدادات كل متجر تُحفظ كـ JSON في عمود `settings` بجدول `Stores` — أضف إعداداً
  جديداً بسطر واحد في `FIELDS` داخل `routes/settings.js` دون تعديل قاعدة البيانات.
- أحداث `app.subscription.*` و`app.installed/uninstalled` تُحدّث حالة الاشتراك
  تلقائياً. الحارس `requireSubscription` جاهز لحماية الميزات المدفوعة.

## الأتمتة (إرسال الرسائل تلقائياً)

هذا هو القلب العملي للتطبيق: "عند حدوث كذا، أرسل هذه الرسالة".

```
lib/triggers.js       الأحداث التي يمكن البناء عليها + كيف نستخرج متغيّرات كل حدث
lib/template.js       استبدال {متغيّر} في نص الرسالة (بلا تنفيذ كود — النص من التاجر)
lib/channels/         قناة لكل وسيلة إرسال: whatsapp · webhook · dry-run
services/automation.js المحرّك: مطابقة القواعد → صندوق الصادر → الإرسال مع إعادة المحاولة
routes/automation.js  واجهة القواعد وصندوق الصادر
routes/tasks.js       /tasks/dispatch لتشغيل الإرسال من Cloud Scheduler
```

### كيف تسير الرسالة

1. يصل حدث من سلة إلى `/webhook`.
2. المحرّك يجد قواعد هذا المتجر المفعّلة لهذا الحدث، ويفحص شروطها.
3. يبني نص الرسالة من القالب، ويضعها في **صندوق الصادر** (`OutboxMessages`).
4. عامل في الخلفية يرسل الرسائل المستحقّة، ويعيد المحاولة عند الفشل
   (تراجع أُسّي: ١ ثم ٥ ثم ٢٥ دقيقة، ثم يستسلم ويسجّل السبب).

الفصل بين ٣ و٤ مقصود: سلة تنتظر رداً سريعاً على الويبهوك، والإرسال قد يفشل
أو يتأخّر. الصندوق يضمن ألّا تضيع رسالة، ويعطي التاجر سجلاً يرى فيه كل شيء.

### القنوات

| القناة | ماذا تحتاج |
|---|---|
| **تجربة فقط** | لا شيء — تسجّل الرسالة ولا ترسلها. القيمة الافتراضية لأي قاعدة جديدة. |
| **ربط خارجي** | `AUTOMATION_WEBHOOK_URL` — يرسل JSON لأي خدمة (Make أو Zapier أو n8n أو مزوّد SMS). |
| **واتساب** | `WHATSAPP_TOKEN` و`WHATSAPP_PHONE_NUMBER_ID` من WhatsApp Cloud API. |

**قيد مهم في واتساب**: Meta لا تسمح بنص حر إلا خلال ٢٤ ساعة من آخر رسالة
أرسلها العميل. خارج هذه النافذة يجب استخدام قالب معتمد — اضبط
`WHATSAPP_TEMPLATE_NAME` حينها.

إضافة قناة = ملف في `lib/channels/` يصدّر `send()` + سطر في `index.js`.
إضافة حدث = عنصر في `TRIGGERS` داخل `lib/triggers.js`.

### الإرسال على Cloud Run

المؤقّت الداخلي يتوقّف إن نامت الحاوية. الحل: اضبط `AUTOMATION_DISPATCH_TOKEN`
واجعل Cloud Scheduler يستدعي كل دقيقة:

```
POST https://your-app/tasks/dispatch
Authorization: Bearer $AUTOMATION_DISPATCH_TOKEN
```

## واجهة المستخدم (UI)

- عربية من اليمين لليسار (RTL)، وضع ليلي/نهاري يُحفظ في المتصفح، تصميم متجاوب
  (قائمة جانبية تتحوّل إلى منسدلة على الجوال).
- نظام التصميم كله في `public/css/app.css` — غيّر لون العلامة من `--brand-*` في أعلى الملف.
- القوالب بلا منطق: كل تنسيق (مبالغ، تواريخ، جمع عربي، ألوان الحالات) عبر فلاتر
  nunjucks معرّفة في `lib/format.js`.
- محرّك القوالب صار nunjucks مباشرة بدل `consolidate` (ليمكن تسجيل الفلاتر).
- الخط: IBM Plex Sans Arabic من Google Fonts مع خطوط النظام كبديل.

## التشغيل والنشر

- `npm run dev` للتطوير، `npm start` للإنتاج، `npm test` للاختبارات.
- `Dockerfile` جاهز لـ Cloud Run: يقرأ `PORT` من البيئة، يعمل بمستخدم غير جذر،
  ويغلق بهدوء عند SIGTERM.
- الجلسات محفوظة في قاعدة البيانات (`connect-session-sequelize`) فلا تضيع عند
  إعادة التشغيل — مهم لأن Cloud Run يعيد تشغيل الحاويات باستمرار.
- `/healthz` لفحص السلامة.
- قبل النشر: اضبط `SESSION_SECRET` بقيمة عشوائية طويلة (التطبيق يرفض الإقلاع
  الصامت ويطبع خطأ إن بقيت القيمة الافتراضية في وضع الإنتاج).

## مستودع متداخل (انتبه!)

- المجلد الفرعي `Opencode/` نسخة مستنسخة منفسة (`git clone`) لنفس المستودع على GitHub، وهي غير متتبعة في المستودع الأب.
- عند تنفيذ أي أمر Git، تأكد أي مستودع تعمل عليه (الأم أم `Opencode/.git`) حتى لا تخلط بين الاثنين.

## بيئة المستخدم (مهم جداً)

- **المستخدم مبتدئ تماماً ويتواصل بالعربية** — اشرح بخطوات بسيطة ومجزأة، وتجنّب المصطلحات التقنية أو اشرحها.
- نظام التشغيل: Windows مع PowerShell 5.1.
- المستخدم يعمل عبر **تطبيق OpenCode Desktop** فقط؛ أمر `opencode` غير متوفر في PowerShell.

## ملاحظات تقنية

- تم تثبيت Git 2.55 في `C:\Program Files\Git\cmd\git.exe` لكنه قد لا يظهر في نافذة PowerShell فُتحت قبل التثبيت — استخدم المسار الكامل.
- المجلد داخل **Dropbox** (`C:\Users\malra\Dropbox\PC\Documents\`) — مزامنة السحابة قد تتعارض مع ملفات البناء المؤقتة (node_modules وغيرها).
